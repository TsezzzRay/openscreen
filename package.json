{
  "name": "openscreen",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build:agent": "node -e \"require('node:fs').rmSync('agent/dist', { recursive: true, force: true })\" && tsc -p agent/tsconfig.json",
    "build:agent-tests": "node -e \"require('node:fs').rmSync('agent/dist-test', { recursive: true, force: true })\" && tsc -p agent/tsconfig.test.json",
    "build:helper": "swift build --product ObservationHelper",
    "test:agent": "npm run build:agent && npm run build:agent-tests && node --test \"agent/dist-test/tests/**/*.test.js\"",
    "dev": "npm run build:agent && npm run build:helper && OPENSCREEN_HELPER_PATH=\"$PWD/.build/debug/ObservationHelper\" swift run OpenScreen"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.80.7",
    "@earendil-works/pi-ai": "0.80.7",
    "@vscode/ripgrep": "^1.18.0",
    "typebox": "1.1.38"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  }
}
