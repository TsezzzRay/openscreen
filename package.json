{
  "name": "openscreen",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "build:agent": "node -e \"require('node:fs').rmSync('agent/dist', { recursive: true, force: true })\" && tsc -p agent/tsconfig.json",
    "test:agent": "npm run build:agent && node --test agent/dist/*.test.js agent/dist/*/*.test.js",
    "dev": "npm run build:agent && swift run OpenScreen"
  },
  "dependencies": {
    "openai": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  }
}
